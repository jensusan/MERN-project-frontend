import {Link} from "react-router-dom";

const NavBar = () => {
    return(
        <nav className="nav">
            <Link to="/">
                <div>TBD Title</div>
            </Link>
        </nav>
    )
};

export default NavBar;